@{
    ViewData["Title"] = "Home Page";
    int? Id = Context.Session.GetInt32("Id");
}

<div class="text-center">

    
    @if (Id != null)
    {
    }
    else
    {
        <div class="container" >
            <div class="row text-center">
                <div class="col-lg-6 col-md-4 col-sm-6 p-5 m-5" style="padding:25px;margin-top:50px;">
                    <a asp-action="login" asp-controller="Customer" class="btn btn-warning my-5 cursive-font">Sign In</a>
                </div>
                <div class="col-lg-6 col-md-4 col-sm-6 p-5 m-5" style="padding:25px;margin-top:50px;">
                    <a asp-action="Register" asp-controller="Customer" class="btn btn-warning my-5 cursive-font">Sign Up</a>
                </div>
            </div>
        </div>
    }

   </div>
